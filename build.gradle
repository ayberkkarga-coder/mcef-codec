plugins {
	id 'net.fabricmc.fabric-loom' version "${loom_version}"
}

repositories {
	mavenCentral()
}

loom {
	mods {
		"mcef-codec" {
			sourceSet sourceSets.main
		}
	}
}

dependencies {
	minecraft "com.mojang:minecraft:${project.minecraft_version}"
	implementation "net.fabricmc:fabric-loader:${project.loader_version}"
	implementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_api_version}"
	// Minecraft's own library (for extracting tar.gz); ships with the game at runtime
	compileOnly "org.apache.commons:commons-compress:1.28.0"
}

processResources {
	def version = project.version
	inputs.property "version", version
	filesMatching("fabric.mod.json") {
		expand "version": version
	}
}

tasks.withType(JavaCompile).configureEach {
	it.options.release = 25
	it.options.encoding = "UTF-8"
	// java-cef fork is old code; warnings must not become errors
	it.options.compilerArgs.addAll(["-Xlint:none", "-nowarn"])
}

// Required by the LGPL: the license texts must be inside the jar as well
jar {
	from("LICENSE") { rename { "META-INF/LICENSE" } }
	from("COPYING.LESSER") { rename { "META-INF/COPYING.LESSER" } }
	from("LICENSE-java-cef") { rename { "META-INF/LICENSE-java-cef" } }
	from("NOTICE") { rename { "META-INF/NOTICE" } }
}

java {
	sourceCompatibility = JavaVersion.VERSION_25
	targetCompatibility = JavaVersion.VERSION_25
}
